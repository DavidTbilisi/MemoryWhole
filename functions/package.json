{
  "name": "mnemonic-premium-functions",
  "private": true,
  "engines": {
    "node": "20"
  },
  "main": "src/index.js",
  "scripts": {
    "lint": "node -e \"console.log('No lint step configured for functions yet.')\"",
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^13.6.0",
    "firebase-functions": "^6.5.0",
    "stripe": "^18.1.1"
  }
}
